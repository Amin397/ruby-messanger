import 'package:get/get.dart';

class GenderModel{
  RxBool isSelected;
  int id;
  String title;
  String animation;

  GenderModel({required this.isSelected,required this.id,required this.title , required this.animation});
}